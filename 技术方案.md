# Agora-MVP-技术实施补丁-v3.1a

> **文档定位**：本文是《Agora-MVP-最终工程规格-v3.1》的施工顺序与验证策略补丁。v3.1 仍然是最终目标规格和唯一核心协议源。本文只定义"先做什么、怎么验证、模块怎么分层"，不改变最终产品定义和核心协议。若与 v3.1 核心契约冲突，以 v3.1 为准。
>
> **精度说明**：本文中的代码片段用于说明职责边界和调用关系，不是新增协议来源。若示例代码与 v3.1 冻结接口细节存在差异，以 v3.1 为准。函数签名、参数名、返回类型以 v3.1 类型定义章节为最终标准。

---

## 0. 修订策略摘要

### 保持不动（来自 v3.1，本文不碰）
- 数据库 11 张表完整 schema
- 状态机白名单迁移规则
- SSE 11 个事件的字段定义和语义
- Prompt 冻结包 5 段正文
- SecretaryRawOutput / SecretaryRawOutputSchema / DiscussionSummaryFinal 命名体系
- 计费口径（raw_cost → platform_price）和 hold/settle/release/refund 语义
- 容错规则（ROUND_RULES、MIN_MODELS_PER_ROUND、TTFT 超时等）
- 44 条测试矩阵（U01-U20、I01-I12、E01-E06、C01-C06）
- 23 条最终完成定义 checklist
- **v3.1 目录结构（`src/lib/`）默认不改**

### 本文新增
- CLI-first 施工策略和正式定位
- Core / Renderer 逻辑分层（不要求物理改目录）
- 事件优先原则（event-first）与事件映射表
- CLI 命令清单（含 `council upgrade` 主链路）
- 显式 `ActorContext` 上下文传参
- 统一 `session-starter` 讨论启动路径
- CLI 阶段 Go/No-Go 验收标准
- 分阶段施工路径（Phase A1/A2/B/C）
- 对 v3.1 Task Graph 的轻量调整建议
- 非目标清单
- CLI → Web 接入规则
- JSONL 事件日志的定位与边界

### 只做补充说明（不改写 v3.1 正文）
- v3.1 的 `onEvent` 回调机制天然支持 event-first，本文只补充 CLI renderer 如何消费
- v3.1 的 `runConsensusDiscussion` 已经是 renderer-agnostic 的，本文只明确分层边界

---

## 1. 为什么采用 CLI-first

### 当前阶段的核心风险不在 UI，在引擎

Agora 的产品价值取决于：匿名互评后第 3 轮是否真的更有料、书记员总结是否真的把分歧讲清楚、系统在多模型脏环境下是否稳定。这三件事全部是引擎问题，不是 UI 问题。

如果先做完整 Web UI，会遇到以下问题：
1. 前端交互复杂度（四宫格流式、进度条、恢复态、降级态）会消耗大量时间
2. 前后端联调会被 SSE 连接管理、Vercel 超时、断线重连等问题拖慢
3. 引擎 bug 和 prompt 质量问题被埋在 UI 层下面，难以快速定位
4. 一旦发现引擎层需要调整，前端也要跟着改

### CLI 更适合先验证引擎

- CLI 直接消费事件流，5 分钟就能跑通完整讨论
- 输出是结构化数据，可以直接做质量评估和回归测试
- 故障定位不需要打开浏览器 DevTools
- prompt 迭代周期从"改 prompt → 部署 → 打开浏览器 → 等 SSE"缩短为"改 prompt → 跑一条命令"

### 但 CLI 不是最终产品

最终 MVP 仍然是 Web 产品（v3.1 定义的 Next.js 全栈应用）。CLI 只是引擎验证器和开发工具。它必须复用与 Web 完全相同的 core 层代码。

---

## 2. CLI 的正式定位

CLI 是以下四种角色的合体：

| 角色 | 用途 |
|------|------|
| **Orchestration Harness** | 在终端直接驱动 runConsensusDiscussion 主流程 |
| **Engine Validator** | 验证 3 轮讨论、匿名互评、容错、书记员总结、会话升级等核心能力 |
| **Debug / Replay Tool** | 回放历史讨论事件流、定位故障、辅助 prompt 迭代 |
| **Test Entrypoint** | 集成测试和 chaos 测试的执行入口 |

**CLI 不是**：
- 对外 MVP 主产品形态
- 独立维护的第二套产品
- 需要精致 TUI 的终端应用

---

## 3. Core / Renderer 分层架构

### 分层原则

v3.1 的 `runConsensusDiscussion` 已经通过 `onEvent: (event: SSEEvent) => void` 回调实现了 renderer-agnostic。本文把这个隐含的分层显式化为逻辑分层规则，**不要求物理修改 v3.1 的 `src/lib/` 目录结构**。

```
┌──────────────────────────────────────────────────────┐
│                 Core 层（逻辑边界）                     │
│  物理位置：src/lib/（沿用 v3.1，不改目录名）             │
│  CLI 和 Web 共用，不允许有两套实现                       │
│                                                       │
│  lib/orchestrator/consensus.ts   — 3 轮编排主流程      │
│  lib/orchestrator/stream-hub.ts  — 容错并行流          │
│  lib/orchestrator/session-starter.ts — 统一启动路径    │
│  lib/orchestrator/anonymizer.ts                       │
│  lib/orchestrator/secretary.ts                        │
│  lib/orchestrator/context-manager.ts                  │
│  lib/orchestrator/execution-lock.ts                   │
│  lib/billing/*    lib/security/*    lib/prompt/*      │
│  lib/openrouter/* lib/db/*          lib/types/*       │
└─────────────────────┬────────────────────────────────┘
                      │
             onEvent(event: SSEEvent)
                      │
           ┌──────────┴──────────┐
           │                     │
    ┌──────┴──────┐    ┌────────┴────────┐
    │ CLI Renderer│    │  Web Renderer   │
    │ src/cli/    │    │  src/app/api/   │
    │             │    │  + SSE adapter  │
    └─────────────┘    └─────────────────┘
```

### 目录结构（增量，不改 v3.1 已有目录）

```
src/
├── lib/                           # ← v3.1 原有目录，保持不动
│   ├── orchestrator/
│   │   ├── consensus.ts
│   │   ├── session-starter.ts     # ← 新增：统一启动路径（见 3.3）
│   │   ├── stream-hub.ts
│   │   ├── anonymizer.ts
│   │   ├── secretary.ts
│   │   ├── context-manager.ts
│   │   ├── execution-lock.ts
│   │   └── quality-evaluation.ts
│   ├── billing/
│   ├── security/
│   ├── openrouter/
│   ├── prompt/
│   ├── db/
│   ├── types/
│   └── observability/
│
├── cli/                           # ← 新增：CLI Renderer
│   ├── index.ts                   # CLI 入口（commander）
│   ├── commands/
│   │   ├── ask.ts                 # 单模型快问（one-shot）
│   │   ├── chat.ts                # 会话化单模型（多轮对话）
│   │   ├── council-run.ts         # 发起议会
│   │   ├── council-upgrade.ts     # 从单模型会话升级为议会
│   │   ├── council-replay.ts      # 回放历史
│   │   ├── council-followup.ts    # 追问
│   │   └── council-export.ts      # 导出
│   ├── display.ts                 # 终端事件渲染（chalk + 品牌色）
│   └── event-logger.ts            # JSONL 事件日志（debug artifact）
│
├── app/                           # ← v3.1 原有，Phase B 接入
└── ...
```

**可选重构**：未来如果团队认为 `src/lib/` → `src/core/` 更清晰，可以作为工程重构项执行。但 v3.1a 不以此为前提，当前阶段 `src/lib/` 完全够用。

### 分层铁律

1. **`src/lib/` 下的代码不得 import `src/cli/` 或 `src/app/` 下的任何模块**
2. **`src/cli/` 和 `src/app/` 可以 import `src/lib/`**
3. **业务逻辑只能写在 `src/lib/`**
4. **CLI 和 Web 不共享 renderer 代码，但共享所有 core 代码**
5. **如果某段逻辑同时被 CLI 和 Web 需要，它必须在 `src/lib/`**

### 3.1 显式 ActorContext（不用 getCurrentUser 模式）

Core 层的所有 workflow 函数不得内部调用"获取当前用户"逻辑。必须通过显式参数接收调用者上下文：

```typescript
// src/lib/types/index.ts
export interface ActorContext {
  userId: string;
  source: 'cli' | 'web' | 'test';
}

// core workflow 签名示例
async function runConsensusDiscussion(params: {
  actor: ActorContext;          // ← 显式传入，不内部获取
  discussionId: string;
  topic: string;
  models: string[];
  billingSnapshotId: string;
  onEvent: (event: SSEEvent) => void;
}) { ... }
```

- CLI 传入 `{ userId: process.env.CLI_TEST_USER_ID, source: 'cli' }`
- Web 传入 `{ userId: session.user.id, source: 'web' }`（从 NextAuth session 解析）
- 测试传入 `{ userId: 'test-user-id', source: 'test' }`

**禁止**：在 `src/lib/` 中 import NextAuth、读取 cookie、调用 `getServerSession()`。这些只能在 `src/app/` 中做。

### 3.2 统一 session-starter（讨论启动唯一路径）

v3.1 中"执行锁获取 + orchestrator 启动"的逻辑必须抽成独立模块，CLI 和 Web 共用：

```typescript
// src/lib/orchestrator/session-starter.ts

export async function startOrAttachDiscussion(params: {
  actor: ActorContext;
  discussionId: string;
  onEvent: (event: SSEEvent) => void;
}): Promise<{ role: 'owner' | 'observer'; discussion: Discussion }> {
  const discussion = await getDiscussion(params.discussionId);
  
  if (discussion.status === 'created') {
    // 尝试获取执行锁
    const lockAcquired = await tryAcquireLock(discussion.id);
    if (lockAcquired) {
      // 本连接成为 owner，启动 orchestrator
      await logExecution(discussion.id, 'started');
      // 在后台启动（不阻塞返回）
      runConsensusDiscussion({ ...params, discussion }).catch(handleFatalError);
      return { role: 'owner', discussion };
    }
  }
  
  // 锁获取失败 / 已在执行中 / 终态 → observer 模式
  return { role: 'observer', discussion };
}
```

**职责分工**：
- `session-starter.ts`：判断 ownership、获取锁、启动 orchestrator
- `handleFatalError`：**不是只记日志**。必须负责：(1) 将 discussion 状态 CAS 迁移到 `failed`（写入 `failed_at` + `error_code` + `error_message`）；(2) 释放执行锁；(3) 根据当前 billing state 执行既定账务收尾逻辑（release 未消耗预占 / refund 已扣减部分 / 或 failed settlement cleanup，具体路径由 v3.1 hold/settle/release/refund 语义决定）；(4) 记录 `discussion_executions` 终态。即：handleFatalError 承担的是 orchestrator catch 块同等的收尾职责，确保不会留下悬挂的 `streaming` 状态或冻结的积分
- Web `GET /stream`：调用 `startOrAttachDiscussion()`，根据 role 决定是推实时流还是返回 restore
- CLI `council-run`：调用 `startOrAttachDiscussion()`，owner 则实时消费事件

**铁律**：
- `GET /api/discussions/:id/stream` 不得直接调用 `runConsensusDiscussion`
- `cli/commands/council-run.ts` 不得直接调用 `runConsensusDiscussion`
- 两者都通过 `session-starter.ts` 走统一启动路径
- 重复连接到同一 discussion 时，`session-starter` 返回 `observer`，不会重复启动

---

## 4. 事件优先原则（Event-First）

### 原则

v3.1 的 orchestrator 已经是 event-driven 的——`onEvent` 回调是唯一的输出通道。本文强化这一原则：

1. **CLI stdout 只是事件流的终端渲染**，不是独立的输出通道
2. **Web SSE 是同一事件流的网络渲染**
3. **replay / 测试 / 调试 都消费同一套事件 schema**
4. **不允许 CLI 直接 `console.log` 不可结构化的信息作为核心输出**

### CLI 事件渲染规则

CLI renderer（`cli/display.ts`）接收 `onEvent` 回调推送的事件，按以下规则渲染到终端：

| v3.1 事件 | CLI 渲染行为 |
|-----------|-------------|
| `progress` | `[Round N/3] 阶段名...` |
| `chunk` | 实时追加模型输出文本（品牌色，`logical_model_id` 标签） |
| `model_done` | `✅ [Claude] 完成 (420 tokens)` |
| `model_error` | `⏰ [DeepSeek] 超时跳过` / `🔄 [Claude] 重试中` / `⚠️ [Claude] 降级为 Haiku` |
| `round_done` | `── Round N 完成 (3/4 模型参与) ──` |
| `anonymize` | `🎭 匿名互评开始 [选手A, 选手B, ...]` |
| `summary` | 结构化渲染共识/分歧/建议（`is_degraded` 时标注降级） |
| `done` | `✅ 讨论完成 | raw: $0.082 | platform: $0.0995` |
| `error` | `❌ 讨论失败: {message}` |
| `restore` | `♻️ 恢复到 Round {last_completed_round}` |
| `interrupt_ack` | `📝 插话已记录，将在下一阶段纳入` |

### JSONL 事件日志（定位与边界）

每次讨论的完整事件流可同时写入本地 JSONL 文件，用于 debug 和 replay：

```typescript
// cli/event-logger.ts
// 追加到 .agora/sessions/{discussionId}.events.jsonl
{ "timestamp": "2026-03-15T12:00:00.123Z", "type": "chunk", "data": { ... } }
{ "timestamp": "2026-03-15T12:00:00.456Z", "type": "model_done", "data": { ... } }
```

**JSONL 的定位（写死）**：
1. JSONL 只是 **CLI 阶段的 replay / debug artifact**，不是 canonical state
2. **Canonical state 以 DB 持久化为准**（conversations + messages + discussion_rounds 表）
3. JSONL 缺失、损坏或不存在 **不影响生产核心流程**
4. **生产环境不依赖本地文件系统 replay**
5. JSONL 主要用途：CLI replay 命令 + Phase B 的 Web UI mock 开发 + prompt 迭代 debug
6. Phase B/C 中 Web 不读取也不写入 JSONL

---

## 5. CLI 命令清单与验证闭环

### 5.1 `agora ask` — 单模型快问（one-shot）

```bash
agora ask "什么是量子计算" --model openai/gpt-5.2
```

| 项目 | 说明 |
|------|------|
| 目标 | 验证 OpenRouter 适配层 + 单模型流式响应 + token 统计 |
| 最小输入 | topic(string) + model(string) |
| 最小输出 | 流式文本 + token 统计 + raw_cost |
| 验证点 | 流式输出完整、token 统计正确、raw_cost 计算正确、输入安全检查生效 |
| 与 Web 关系 | 同一个 `lib/openrouter/client.ts` + `lib/security/risk-control.ts` |

### 5.2 `agora chat` — 会话化单模型（多轮对话）

```bash
agora chat --model openai/gpt-5.2
# 进入交互式会话：
# > 什么是量子计算
# [GPT] 量子计算是...
# > 它和经典计算有什么区别
# [GPT] 主要区别在于...
# > /switch anthropic/claude-sonnet-4.6
# [切换模型为 Claude]
# > 继续解释量子纠缠
# [Claude] 量子纠缠是指...
# > /upgrade
# [升级为议会讨论，见 council upgrade]
```

| 项目 | 说明 |
|------|------|
| 目标 | 验证会话化对话 + 可变模型切换 + 上下文保持 + 升级入口 |
| 最小输入 | 初始 model(string)，后续通过交互输入 |
| 最小输出 | 多轮对话记录（conversations + messages 持久化到 DB） |
| 验证点 | 中途 `/switch` 切换模型不丢上下文、每条消息带 `logical_model_id`、conversation 记录正确、`/upgrade` 触发 council upgrade 流程 |
| 与 Web 关系 | 同一个 conversations/messages 数据模型、同一个可变模型对话逻辑 |

**交互命令**：
- `/switch <model>` — 切换模型
- `/upgrade` — 升级为议会（触发 council upgrade）
- `/exit` — 退出会话

### 5.3 `agora council run` — 发起完整议会讨论

```bash
agora council run "AI 是否会取代程序员" 
agora council run "比特币是否值得投资" --models claude,gpt,gemini,deepseek
agora council run "..." --dry-run  # 只预估成本不执行
```

| 项目 | 说明 |
|------|------|
| 目标 | 验证完整 3 轮共识模式 |
| 最小输入 | topic(string)，可选 models |
| 最小输出 | 3 轮模型输出 + 匿名标签 + DiscussionSummaryFinal JSON + 成本统计 |
| 验证点 | 状态机迁移正确、匿名互评无模型名泄露、SecretaryRawOutputSchema 校验通过、容错生效、CompressedRoundState 保留关键证据、token/cost 追踪正确 |
| 与 Web 关系 | 同一个 `lib/orchestrator/consensus.ts`，Web 只是换了 renderer |

### 5.4 `agora council upgrade` — 从单模型会话升级为议会

```bash
agora council upgrade <chatConversationId>
agora council upgrade <chatConversationId> --models claude,gpt,gemini,deepseek
# 也可以在 agora chat 中用 /upgrade 触发
```

| 项目 | 说明 |
|------|------|
| 目标 | 验证 v3.1 的"单模型快问 → 一键升级议会"主链路 |
| 最小输入 | 现有 chat conversation ID + 可选 models |
| 最小输出 | 新的 council 类型 conversation（parent_id 指向原 chat） |
| 验证点 | 见下方详细流程 |
| 与 Web 关系 | 同一个 upgrade 逻辑。Web 侧需要一个"从 chat 升级到 council"的 API 入口；若 v3.1 尚未冻结具体路由名，则实现时与既有路由契约对齐，或列为需人工确认项 |

**upgrade 执行流程**：
1. 读取原 chat conversation 的最近 N 条 messages（N=10，可配置）
2. 用 ContextManager 压缩为 topic summary（作为议会讨论的 topic）
3. 创建新 conversation：`type='council'`, `parent_id=原chatId`, `topic=压缩后的摘要`
4. 执行标准 `runConsensusDiscussion`（3 轮共识模式）
5. 新 conversation 的 `fork_point_message_id` 指向原 chat 的最后一条消息

**验证点**：
- `parent_id` 正确关联
- 压缩后的 topic 保留原对话关键信息
- 新讨论的状态机独立于原 chat
- 原 chat 不受影响（status 不变）

### 5.5 `agora council replay` — 回放历史讨论

```bash
agora council replay {discussionId}
agora council replay --last
```

| 项目 | 说明 |
|------|------|
| 目标 | 验证事件流完整性 + 辅助 prompt 迭代和 debug |
| 最小输入 | discussionId 或 --last |
| 最小输出 | 按时间戳重放事件流到终端 |
| 验证点 | JSONL 事件文件完整、replay 输出与首次执行一致 |
| 与 Web 关系 | replay 数据可直接用于 Web UI mock 开发 |

### 5.6 `agora council followup` — 对已有讨论追问

```bash
agora council followup {discussionId} --mode ask_secretary "最大的风险是什么"
agora council followup {discussionId} --mode ask_model --model openai/gpt-5.2 "展开说说"
```

| 项目 | 说明 |
|------|------|
| 目标 | 验证 followup 三种模式（ask_secretary / ask_model / new_council） |
| 最小输入 | discussionId + mode + content |
| 最小输出 | 流式回答 |
| 验证点 | secretary 基于 summary 回答、指定模型带上下文回答 |
| 与 Web 关系 | 同一个 followup 逻辑 |

### 5.7 `agora council export` — 导出结构化总结

```bash
agora council export {discussionId} --format json
agora council export {discussionId} --format markdown
```

| 项目 | 说明 |
|------|------|
| 目标 | 验证 DiscussionSummaryFinal 的消费完整性 |
| 最小输入 | discussionId |
| 最小输出 | JSON 或 Markdown |
| 验证点 | JSON 字段与 v3.1 类型定义一致、Markdown 渲染完整 |
| 与 Web 关系 | Web 的 SummaryCard 消费同样的 DiscussionSummaryFinal |

---

## 6. CLI 阶段验收标准（Go / No-Go）

### 必须满足（Go 条件）

| # | 标准 | 验证方式 |
|---|------|---------|
| G01 | `agora ask` 稳定跑通，流式输出完整 | 手动 + 自动化 |
| G02 | `agora chat` 多轮对话 + `/switch` 切换模型不丢上下文 | 手动 |
| G03 | `agora council run` 4 模型 3 轮完整跑通 | 手动运行 3 场以上 |
| G04 | `agora council upgrade` 从 chat 升级为 council，parent_id 正确 | 手动 + DB 验证 |
| G05 | 匿名互评生效（第 2 轮输出无模型名） | 检查 Round 2 输出 |
| G06 | Secretary 输出通过 SecretaryRawOutputSchema 校验 | U12 通过 |
| G07 | Secretary 降级模式可触发 | U13 通过 |
| G08 | 任一模型 timeout → skip → 讨论继续 | I01 通过 |
| G09 | 2 模型失败 → 讨论继续 | I02 通过 |
| G10 | 3 模型失败 → 讨论失败 + refund | I03 通过 |
| G11 | 状态机迁移严格（终态不可回退） | U01, U02 通过 |
| G12 | hold/settle/release/refund 账本语义正确 | U05-U09 通过 |
| G13 | session 持久化到 DB | 查询 DB 验证 |
| G14 | JSONL 事件日志可生成 | 检查文件存在且可解析 |
| G15 | `agora council replay` 可回放 | 手动验证 |
| G16 | token / raw_cost / platform_price 追踪正确 | 对照 billing_snapshots |
| G17 | 事件字段与 v3.1 SSE 事件定义一致 | 逐字段对照 |
| G18 | 核心协议字段不再频繁变更 | 连续 3 场讨论结构一致 |
| G19 | session-starter 统一启动路径验证 | 重复调用不重复启动 |

### 最好满足（加分项）

| # | 标准 |
|---|------|
| N01 | `agora council followup` 三种模式可用 |
| N02 | 支持 `--mock-provider` 用确定性输出做回归测试 |
| N03 | 插话注入可用 |
| N04 | deterministic replay |

---

## 7. 分阶段施工路径

### Phase A1：最小引擎闭环验证

**目标**：用最少代码跑通"单模型问答 + 4 模型议会讨论"核心链路。

**范围**：
- DB schema + migrations + seed data（11 张表）
- OpenRouter 适配层
- 安全层（输入校验 + 限流基础版）
- Orchestrator 核心（状态机 + 3 轮共识）
- StreamHub 容错（timeout/retry/skip/degraded）
- Anonymizer
- Secretary（zod 校验 + 降级）
- ContextManager（结构化压缩）
- session-starter（统一启动路径）
- CLI 骨架 + `agora ask` + `agora council run`
- JSONL 事件日志

**交付物**：
- `agora ask` 跑通
- `agora council run` 完整 4 模型 3 轮跑通
- 事件流写入 JSONL
- DB 持久化正确

**不做**：
- 计费系统（A1 阶段不 hold/settle）
- 会话化 chat + upgrade
- followup / replay / export
- 任何前端
- 精致的终端交互

**Go 条件**：G01, G03, G05, G06, G07, G08, G09, G11, G13, G14, G17

**预估时间**：4-5 天

**执行策略**：A1 中列出的模块以最小可运行实现为准。安全层只需输入校验基础版（注入 pattern + 长度上限），容错只需覆盖 timeout→skip 主路径，压缩只需产出合法 CompressedRoundState。边界 case 加固、完整覆盖测试和极端场景处理统一延后到 A2。A1 的目标是点火跑通闭环，不是把每个模块做到最终态。

### Phase A2：工程化加固与完整 CLI

**目标**：补全计费、升级链路、replay、测试，达到 CLI 全面验收。

**范围**：
- 计费系统（hold/settle/release/refund）
- `agora chat`（会话化多轮）+ `agora council upgrade`（升级链路）
- `agora council replay` + `agora council export` + `agora council followup`
- 单元测试 U01-U20
- 集成测试 I01-I04, I06-I12（跳过 I05 SSE 断线）
- Chaos 测试 C01-C03, C06（跳过 C04/C05 Vercel 相关）

**交付物**：
- CLI 全命令可用
- 所有 core 模块测试通过
- 3+ 场完整讨论的 JSONL replay 文件
- G01-G19 全部满足

**不做**：
- Next.js / Vercel / SSE / 前端 / 认证 / 管理后台

**预估时间**：3-5 天

### Phase B：Web 最小壳接入

**目标**：把同一引擎接到 Web，验证 SSE 链路和前端核心交互。

**范围**：
- Next.js 项目初始化 + API Routes
- Google OAuth（NextAuth.js v5）
- `POST /api/discussions` + `GET /api/discussions/:id/stream` + `GET /api/discussions/:id`
- SSE adapter：`session-starter` → SSE 推送
- 核心前端页面：`/chat`（单模型 + 议会详情）
- SSE 恢复契约：restore 事件 + can_stream + polling fallback
- 集成测试 I05（SSE 断线恢复）
- Chaos 测试 C04, C05
- E2E 测试 E01, E06

**交付物**：
- 浏览器中跑通完整讨论
- SSE 实时流 + 断线恢复正常
- 四宫格/时间线 + SummaryCard + RestoreBanner 基础版

**不做**：Landing Page、Explore、Settings、管理后台、Stripe、国际化

**预估时间**：5-7 天

### Phase C：产品化完善

**目标**：补全 v3.1 剩余 P0 功能，达到最终完成定义。

**范围**：
- Landing Page、Explore + OG、Settings + Billing、Admin
- Followup API + FollowupBar、Share + Rate
- 国际化、剩余 E2E 测试（E02-E05）
- 部署到 Vercel
- v3.1 第二十二章最终完成定义全部 checklist

**预估时间**：7-10 天

---

## 8. 对 v3.1 Task Graph 的轻量调整建议

v3.1 的 Task-001 ~ Task-022 仍然有效。本文只建议以下调整：

### 8.1 Phase A1 任务组

| 新 Task | 名称 | 前置 | 完成标准 |
|---------|------|------|---------|
| Task-001a | CLI 骨架 + event-logger | Task-001 | `agora --help` 输出正常 + JSONL 写入可用 |
| Task-002a | session-starter 统一启动路径 | Task-002, Task-008 | CLI 和 Web 共用同一个启动入口，重复调用不重复启动 |
| Task-A1-E2E | Phase A1 端到端验证 | Task-001a ~ Task-012 | `agora council run` 完整跑通，G01/G03/G05-G09/G11/G13/G14/G17 满足 |

### 8.2 Phase A2 任务组

| 新 Task | 名称 | 前置 | 完成标准 |
|---------|------|------|---------|
| Task-A2-chat | CLI chat + upgrade | Task-A1-E2E, Task-007 | `agora chat` 多轮 + `/upgrade` → council，G02/G04/G12 满足 |
| Task-A2-tools | CLI replay + export + followup | Task-A1-E2E | 三个命令可用，G15 满足，N01 满足 |
| Task-A2-test | CLI 全面测试 | Task-A2-chat, Task-A2-tools | U01-U20 + I01-I04/I06-I12 + C01-C03/C06 全部通过，G01-G19 全部满足 |

### 8.3 将 Task-015 拆成两层

| Task | 名称 | 前置 | 完成标准 |
|------|------|------|---------|
| Task-015-CLI | CLI 端到端联调 | Task-A2-test | CLI 完整链路跑通 + 所有 Phase A 测试通过 |
| Task-015-Web | Web 端到端联调 | Task-015-CLI + Task-016 | 浏览器跑通 + SSE 恢复正常 + E01/E06 通过 |

### 8.4 新增事件一致性验证

| Task | 名称 | 前置 | 完成标准 |
|------|------|------|---------|
| Task-A2-event | 事件契约一致性检查 | Task-A1-E2E | CLI 事件字段与 v3.1 SSE 事件定义逐字段对齐 |

### 8.5 调整后的执行顺序

```
Phase A1（最小引擎闭环）:
  Task-001 → Task-002 → Task-004 → Task-005 → Task-001a
  → Task-008 → Task-009 → Task-010 → Task-011 → Task-012
  → Task-002a → Task-A1-E2E

Phase A2（工程化加固）:
  Task-007 → Task-A2-chat → Task-A2-tools
  → Task-A2-event → Task-A2-test → Task-015-CLI

Phase B（Web 最小壳）:
  Task-003 → Task-006 → Task-013 → Task-016(核心页面) → Task-015-Web

Phase C（产品化）:
  Task-016(剩余) → Task-017 → Task-018 → Task-019 → Task-020 → Task-021 → Task-022
```

注意：Task-003（认证）和 Task-007（计费）延迟到各自需要的阶段。CLI A1 阶段用 `.env` 配 test user ID，不需要 OAuth 也不需要 hold/settle。

---

## 9. 非目标（明确不做）

| # | 非目标 | 原因 |
|---|--------|------|
| X01 | 独立 CLI 产品设计 | CLI 是内部工具，不是产品 |
| X02 | 复杂 TUI（blessed/ink） | chalk 着色够用 |
| X03 | CLI 专属状态机 | 复用 v3.1 |
| X04 | CLI 专属 summary schema | 复用 SecretaryRawOutput → DiscussionSummaryFinal |
| X05 | CLI 专属 prompt 体系 | 复用 v3.1 冻结包 |
| X06 | CLI 专属事件定义 | 复用 v3.1 SSE 事件字段 |
| X07 | CLI 专属 DB schema | 复用 v3.1 的 11 张表 |
| X08 | 与 Web 断裂的临时逻辑 | 所有 lib 代码必须 Web 可复用 |
| X09 | CLI 用户认证系统 | CLI 阶段用 `.env` 配 test user |
| X10 | CLI 支付/计费 UI | 计费逻辑在 lib 层验证 |
| X11 | `src/lib/` → `src/core/` 物理重构 | 可选工程改进项，不是 v3.1a 前提 |
| X12 | JSONL 作为 canonical state | DB 才是 canonical state |

---

## 10. 从 CLI 迁移到 Web 的接入规则

### Web 不得重写 orchestration

Web API Route 必须调用 `src/lib/orchestrator/session-starter.ts` 的 `startOrAttachDiscussion()`。禁止在 API Route 中重新实现执行锁获取或编排启动逻辑。

```typescript
// src/app/api/discussions/[id]/stream/route.ts
export async function GET(req, { params }) {
  // 1. 从 Web auth 上下文解析 session（NextAuth 只在 app/ 层调用，core 不依赖）
  const session = await getServerSession(authOptions);
  if (!session?.user?.id) {
    return Response.json({ error: { code: 'UNAUTHORIZED', message: '请先登录' } }, { status: 401 });
  }
  
  // 2. 显式构造 ActorContext（core 通过参数接收，不内部获取）
  const actor: ActorContext = { userId: session.user.id, source: 'web' };
  
  // 3. 通过统一启动路径判断 ownership
  const { role, discussion } = await startOrAttachDiscussion({
    actor,
    discussionId: params.id,
    onEvent: (event) => { /* SSE 推送 */ },
  });
  
  if (role === 'owner') {
    return sseStream(/* ... */);
  } else {
    return restoreResponse(discussion);
  }
}
```

### 前端状态完全来源于事件和状态机

Web 前端不得自创状态。所有 UI 状态来源于 SSE 事件流或 `GET /api/discussions/:id` 返回的 DB 记录。

### CLI replay 数据可辅助 Web 开发

Phase A 产出的 JSONL 文件可用于 Web UI mock 开发和 SSE adapter 联调。但这是便利性工具，不是依赖。Web 不得依赖 JSONL 文件存在。

---

## 11. 【需人工确认的冲突点】

### 冲突 1：v3.1 Task-003 认证是 Task-006 的前置依赖

v3.1 的 Task-006（单模型聊天）依赖 Task-003（认证）。但 CLI A1 阶段不需要 Google OAuth。

**建议处理**：CLI 阶段通过 `ActorContext` 传入 `{ userId: process.env.CLI_TEST_USER_ID, source: 'cli' }`，跳过认证。Phase B 接入 Web 时恢复 Task-003 → Task-006 依赖关系。

### 冲突 2：v3.1 的 GET /stream 描述中执行锁获取内联在 API Route

v3.1 的 `GET /api/discussions/:id/stream` 行为规则描述中，执行锁获取和 orchestrator 启动是该 Route 的职责。

**建议处理**：抽出 `session-starter.ts`，Route 只调用它。v3.1 的行为规则语义不变（锁获取 → 成功则启动 → 失败则 restore），只是实现位置从 Route 内移到共享模块。建议在 v3.1 第十一章补一句说明。

### 冲突 3：v3.1 Orchestrator 签名中无 ActorContext

v3.1 的 `runConsensusDiscussion` 接收 `userId: string`，而本文建议改为 `actor: ActorContext`。

**建议处理**：这是向后兼容扩展。`ActorContext.userId` 等价于原来的 `userId`。建议在 v3.1 中将参数类型更新为 `ActorContext`，原有语义不受影响。

---

## 12. CLI 阶段的最小技术栈

| 层级 | 选型 | 说明 |
|------|------|------|
| CLI 框架 | commander | 命令解析 |
| 终端渲染 | chalk | 品牌色 + 基础格式化 |
| DB | PostgreSQL (Supabase) | 与 v3.1 相同 |
| ORM | Drizzle | 与 v3.1 相同 |
| API | OpenRouter | 与 v3.1 相同 |
| Schema 校验 | zod | 与 v3.1 相同 |
| 事件日志 | JSONL 文件 | `.agora/sessions/{id}.events.jsonl`（debug artifact） |
| 测试 | vitest | 单元 + 集成 |
| 运行 | tsx | TypeScript 直接执行 |

**不引入**：Next.js、React、Tailwind、shadcn、NextAuth（Phase A 不需要）。

---

> **本文档是 v3.1 的施工顺序补丁，不是替代品。核心协议（schema、状态机、SSE 事件、Prompt、Summary 命名、计费口径）以 v3.1 为准。本文只改变"先做什么、怎么验证"。**

---

# 附录：建议并入 v3.1 的最小修改点

| 序号 | 改什么 | 改到 v3.1 哪里 | 改动量 |
|------|--------|---------------|--------|
| M01 | 新增 `src/cli/` 目录（不改 `src/lib/`） | 第十一章目录结构 | 新增 1 个目录 |
| M02 | 新增 `session-starter.ts` | 第十一章核心服务 | 新增 1 个模块 |
| M03 | `runConsensusDiscussion` 参数增加 `ActorContext` | 第十一章 Orchestrator | 参数类型变更 |
| M04 | Task Graph 新增 Phase A 任务组 | 第二十章 | 新增 ~8 个 Task |
| M05 | Task-015 拆成 CLI/Web 两层 | 第二十章 | 拆分 1 个 Task |
| M06 | 补充"CLI 不走 SSE，事件通过 JSONL 日志" | 第十章 | 1 段话 |
| M07 | 测试矩阵增加 CLI 事件一致性检查 | 第十九章 | 新增 1-2 条 |

**推荐**：维持 v3.1 + v3.1a 双文档模式。v3.1 是"做什么"，v3.1a 是"先做什么"。
